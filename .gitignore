# Secrets — NEVER commit. The repo must go PUBLIC to submit to the hackathon.
.env
.env.local
*.env
**/.env

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Node / web
node_modules/
dist/
.next/
build/

# Data dumps (FEC bulk files are huge — keep them local)
*.csv
*.zip
data/

# Auto-match runtime state (regenerate with `auto_match.py seed`)
server/campaigns.json

# OS / editor
.DS_Store
.vscode/
.idea/
